<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Winnie's Lawyers - Web Server File System</title>
    <link rel="stylesheet" href="../public/Style/styles.css">
</head>
<body>

    <header class="site-header">
        <h1>WINNIE'S <span class="green-text">LAWYERS</span></h1>
        <nav class="site-nav">
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="#">File System</a></li>
                <li><a href="#">Legal Resources</a></li>
                <li><a href="#">Contact Us</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <h2 class="subtitle">Uploaded files</h2>

        <section class="server-info">
            <p><strong>Server Status:</strong> <span class="status">Online</span></p>
            <p><strong>Last Backup:</strong> 2024-09-10 03:15:45</p>
            <p><strong>Current User:</strong> <span id="current-user">User</span></p>
        </section>
        
        <section class="file-system">
            <label for="file-search">Search File System</label>
            <div class="search-container">
                <input type="text" id="file-search" name="file-search" placeholder="Enter file name or directory...">
                <button type="button" onclick="searchFiles()">Search</button>
            </div>
        </section>

        <section class="file-actions">
            <button type="button" onclick="createFolder()">Create Folder</button>
            <button type="button" onclick="deleteSelected()">Delete Selected</button>
        </section>

        <section class="file-list">
            <label for="file-directory">File Directory</label>
            <table id="file-directory">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Type</th>
                        <th>Contact</th>
                        <th>Attachment</th>
                    </tr>
                </thead>
                <tbody id="file-tbody">
                    <!-- Default directory structure -->

                </tbody>
            </table>
        </section>
    </main>

    <footer class="site-footer">
        <p>&copy; 2009 Winnie's Lawyers. All rights reserved.</p>
    </footer>

    <script src="../public/Javascript/honeypot.js"></script>
    <script>
        const data = <%- JSON.stringify(data) %>;
        const fileTbody = document.getElementById('file-tbody');

        data.forEach((item, index) => {
            const row = document.createElement('tr');
            row.innerHTML = `
                <td>${item.name}</td>
                <td>${item.type}</td>
                <td>${item.contact || '--'}</td>
                <td><a style="cursor: pointer;" href=${item.attachment}>${item.attachment || '--'}</a></td>
            `;
            fileTbody.appendChild(row);
        });
    </script>
</body>
</html>
